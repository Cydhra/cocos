[package]
name = "bench"
version = "0.1.0"
edition = "2024"

[dependencies]
libcocos = { path = "../libcocos", features = ["rayon"] }
cocos-parse = { path = "../cocos-parse" }

[dev-dependencies]
rstest = "0.26.1"